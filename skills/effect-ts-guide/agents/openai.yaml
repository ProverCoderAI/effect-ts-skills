interface:
  display_name: "Effect TS Guide"
  short_description: "Effect-TS guidance, review, and checks"
  default_prompt: "Use $effect-ts-guide for Effect-TS implementation or review tasks, start with `npx @prover-coder-ai/effect-ts-check .`, and then apply the manual rules for architecture and typed-effect code."

policy:
  allow_implicit_invocation: true
