interface:
  display_name: "Effect TS Guide"
  short_description: "Effect-TS guidance, checks, and editor setup"
  default_prompt: "Use $effect-ts-guide for Effect-TS implementation, review, or editor setup tasks, start with `bash scripts/run-effect-ts-check.sh .` for Effect code compliance, and narrow the scope when the repository is primarily tooling or fixtures."

policy:
  allow_implicit_invocation: true
